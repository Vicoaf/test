# PROXTEL Laravel Starter Overlay

This is an overlay, not a fake Laravel installation.

Bootstrap the real Laravel framework in the target project using the approved Composer workflow, then apply this overlay and the Agency Skills.

Do not commit `.env` secrets. Database mutations, migrations, deployments and Laravel Boost installation require explicit project authorization.